import React from "react";

class ArtList extends React.Component {
    render() {
       /*const userList = users.map((user,i) => {
            return (
                <li key={i} 
                >{user.name}</li>
            )
        })*/
    }
}